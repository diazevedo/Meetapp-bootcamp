import FileController from '../app/controllers/FileController';

test('It is an instence of FileController', () => {
  expect(FileController).toBeDefined();
});

test('It is an instence of FileController', () => {
  expect(FileController.store()).toBeDefined();
});
